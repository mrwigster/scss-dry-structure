interface:
  display_name: "SCSS DRY Structure"
  short_description: "Enforce SCSS structure, token reuse, and convention-matching selectors"
  default_prompt: "Implement this style change in SCSS by reusing existing variables/mixins, keeping nesting maintainable, and matching established naming conventions (including BEM where present)."
